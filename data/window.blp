using Gtk 4.0;

template $MyGtkAppWindow: Gtk.ApplicationWindow {
	title: "Whisker";
	default-width: 600;
	default-height: 400;

	Box {
		orientation: vertical;
		spacing: 10;

		Button button {
			label: "Click me!";
		}
	}
}
